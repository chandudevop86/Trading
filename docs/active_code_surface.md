# Active Code Surface

Supported local legacy operator surface:
- UI: `streamlit run src/Trading.py`
- Local launcher: `tools/run_app.ps1`
- Docker local profile: `deploy/docker/Dockerfile` and `deploy/docker/docker-compose.yml`

Do not treat `src/breakout_app.py`, backup files, temp files, or snapshot/reference files as supported deployment targets. See `src/EXPERIMENTAL_SURFACE.md`.
