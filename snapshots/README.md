# Snapshot Surface

This directory is reference-only.

Each snapshot captures an older project state for rollback or comparison, but nothing under `snapshots/` is part of the active legacy runtime or supported deployment surface.

Use these active surfaces instead:
- `src/Trading.py`
- `tools/run_app.ps1`
- `docs/active_code_surface.md`
